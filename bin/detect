#!/bin/sh

# Unfortunately, it is impossible to now at this stage 
# if the project is valid or not.
# It would be possible for the easy part (i.e. single crate at root),
# but for everything else, especially submodules, it is impossible.

exit 0
